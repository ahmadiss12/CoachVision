# Runtime dependencies for the deployed API.
#
# The mobile app runs MediaPipe on-device and sends pose landmarks over the
# WebSocket, so the server never decodes video. OpenCV and MediaPipe are
# therefore NOT installed here -- see requirements-serverpose.txt if you need
# the `frame` (JPEG upload) path, which does server-side pose detection.

# AI / numeric
numpy>=1.24.0
xgboost>=2.1.4,<3.0   # pulls in scipy; required by the squat form classifier

# Backend API
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sqlalchemy>=2.0.0
alembic>=1.13.0
psycopg[binary]>=3.2.0
python-multipart>=0.0.9
pydantic>=2.8.0
pydantic-settings>=2.4.0
pyjwt>=2.9.0
passlib[bcrypt]>=1.7.4
